/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cloudflare-ipfs.com",
      "oaidalleapiprodscus.blob.core.windows.net"
    ],
  },
};
module.exports = nextConfig;
